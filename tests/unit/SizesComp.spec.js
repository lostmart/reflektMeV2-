import { shallowMount } from '@vue/test-utils'
import SizesComp from '@/components/SizesComp.vue'

describe('ModalBody.vue', () => {
	it('renders all sizes / items needed', () => {
		const wrapper = shallowMount(SizesComp)
	})
})
